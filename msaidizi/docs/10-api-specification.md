# 10 — API Specification

## Purpose of this document
This document will specify the REST API surface exposed by the NestJS backend: endpoints, request/response shapes, status codes, and versioning approach.

## What will go here (future work)
- Endpoint list per module (auth, users, providers, services, bookings, etc.)
- Request/response schemas
- Error response conventions
- Authentication/authorization requirements per endpoint

## Status
NOT STARTED. To be completed alongside backend module implementation.
