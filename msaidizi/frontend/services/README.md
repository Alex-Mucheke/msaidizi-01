# services/ (frontend API layer)

Frontend-side service functions that call the backend REST API (e.g. authService, providerService, bookingService). This is the ONLY layer that should call the backend — components should not call fetch/axios directly. PLANNED — not implemented yet.
