# prisma/

PLANNED. Will contain the Prisma schema (`schema.prisma`), migrations, and seed scripts once the database design (see docs/09-database-design.md) is complete. No schema has been defined yet — do not invent one here.
