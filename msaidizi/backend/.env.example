# PLANNED — not consumed by any code yet.
PORT=3001

# Database (will be used once Prisma is introduced)
DATABASE_URL="postgresql://user:password@localhost:5432/msaidizi?schema=public"

# Auth (will be used once the auth module is implemented)
JWT_SECRET=
JWT_EXPIRES_IN=1d
